# Project-specific Pi runtime dependencies
# Add libraries needed only by THIS project.
# Run `pip install -r requirements.txt` on the Raspberry Pi.

# Inherited from root requirements.txt (already installed on Pi)
# Remove any lines here that are already in the root requirements.txt
# to avoid version conflicts.

python-dotenv>=1.0.0
